export interface DefaultTheme {
  color: { white: string; black: string; content: string };
}

const theme: DefaultTheme = {
  color: {
    white: "#ffffff",
    black: "#000000",
    content: "lightgray",
  },
};

export default theme;
